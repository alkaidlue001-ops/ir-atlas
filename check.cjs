const fs=require('node:fs'),path=require('node:path'),{execFileSync}=require('node:child_process');let count=0;function check(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){if(e.name==='.local'||e.name==='node_modules')continue;const p=path.join(dir,e.name);if(e.isDirectory())check(p);else if(/\.(c?js)$/.test(p)){execFileSync(process.execPath,['--check',p],{stdio:'pipe'});count++}}}check(__dirname);console.log(count+' scripts: syntax OK');
